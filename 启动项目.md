# CDrive Cleaner 启动与手测

## 启动

```powershell
cd D:\OneDrive\Desktop\CdriveClean\cdrive-cleaner
npm install
npm run tauri dev
```

如果依赖已经装过，后面只需要：

```powershell
cd D:\OneDrive\Desktop\CdriveClean\cdrive-cleaner
npm run tauri dev
```

## 建议测试顺序

1. 普通权限启动应用。
2. 选择 `C:`，点击“开始扫描”，记录扫描完成 toast 和终端日志。
3. 如果页面提示“开启管理员模式”，点击它，Windows UAC 弹窗里点“是”。
4. 管理员模式重启后，再对 `C:` 扫一次，记录是否显示 `MFT + USN`。
5. 在扫描结果里找一个明确可迁移的大目录，迁移到 `D:`。
6. 记录迁移前后 `C:` 和 `D:` 可用空间变化。
7. 打开迁移历史，执行一次回滚，确认数据恢复。

## 重点看什么

- 扫描：
  - 普通权限时是否明确提示会回退到原生目录枚举。
  - 管理员模式后是否真的走 `MFT + USN`。
  - 扫描时间、文件数、目录数、漏算量是否合理。

- 安全检测：
  - 打开迁移弹窗时是否先做安全分析。
  - 风险等级、原因、建议是否符合目录类型。
  - 危险目录是否阻止迁移。

- 迁移：
  - 复制是否成功。
  - 是否创建了链接占位。
  - `C:` 可用空间是否增加。
  - `D:` 是否出现目标数据。
  - 回滚后是否恢复原状。

## 终端日志关键字

- 深度扫描：`[scan-deep]`、`[deep-scan]`、`[阶段0]`、`[阶段1]`
- 迁移：`[migration]`、`[migration-core]`
- 管理员深扫回退：`[mft-usn]`
- USN / 权限 / 漏算定位：`[winfs]`、`enabled privileges`、`captured USN checkpoint`、`metadata path fallback hits`
